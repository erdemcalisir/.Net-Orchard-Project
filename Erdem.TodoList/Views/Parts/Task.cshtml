@{
    var taskTitle = (string)Model.TaskTitle;
    var taskContent = (string)Model.TaskContent;
    var taskStatu = (bool)Model.TaskStatu;
}
    <article>
        TaskTitle: @taskTitle<br />
        TaskContent: @taskContent <br />
        TaskStatu: @taskStatu
    </article>