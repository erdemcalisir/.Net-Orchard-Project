
@{
    var taskId = (int)Model.TaskId;
}
@using (Html.BeginFormAntiForgeryPost(Url.Action("Add", "TaskListCart", new { id = taskId, area = "Erdem.TodoList" })))
{
    <button type="submit">@T("Add to tasklistcart")</button>
}

