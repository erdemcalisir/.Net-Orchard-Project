@{
    var title = (string)Model.Title;
    var content = (string)Model.Content;
    var statu = (bool)Model.Statu;
}
    <article>
        Title: @title<br />
        Content: @content<br />
        Statu: @statu
    </article>